spring:
  profiles:
    active: dev
  application:
    name: nacos-server
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        namespace: test
      discovery:
        namespace: test